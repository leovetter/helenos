import { OnLoadingAboutSectionDirective } from './on-loading-about-section.directive';

describe('OnLoadingAboutSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new OnLoadingAboutSectionDirective();
    expect(directive).toBeTruthy();
  });
});
