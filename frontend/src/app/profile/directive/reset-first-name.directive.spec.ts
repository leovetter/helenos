import { ResetFirstNameDirective } from './reset-first-name.directive';

describe('ResetFirstNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ResetFirstNameDirective();
    expect(directive).toBeTruthy();
  });
});
