import { ResetLastNameDirective } from './reset-last-name.directive';

describe('ResetLastNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ResetLastNameDirective();
    expect(directive).toBeTruthy();
  });
});
