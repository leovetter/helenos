import { CropSecondDirective } from './crop-second.directive';

describe('CropSecondDirective', () => {
  it('should create an instance', () => {
    const directive = new CropSecondDirective();
    expect(directive).toBeTruthy();
  });
});
