import { HttpErrorResponse } from '@angular/common/http';

// Define the type of actions for this feature
export enum UserActionTypes {
}


// define the actions type for this feature
export type UserActions = null;