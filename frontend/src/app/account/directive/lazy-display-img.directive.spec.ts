import { LazyDisplayImgDirective } from './lazy-display-img.directive';

describe('LazyDisplayImgDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyDisplayImgDirective();
    expect(directive).toBeTruthy();
  });
});
