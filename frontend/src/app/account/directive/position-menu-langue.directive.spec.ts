import { PositionMenuLangueDirective } from './position-menu-langue.directive';

describe('PositionMenuLangueDirective', () => {
  it('should create an instance', () => {
    const directive = new PositionMenuLangueDirective();
    expect(directive).toBeTruthy();
  });
});
