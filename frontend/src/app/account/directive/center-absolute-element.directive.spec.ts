import { CenterAbsoluteElementDirective } from './center-absolute-element.directive';

describe('CenterAbsoluteElementDirective', () => {
  it('should create an instance', () => {
    const directive = new CenterAbsoluteElementDirective();
    expect(directive).toBeTruthy();
  });
});
