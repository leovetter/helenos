import { OnLoadingPlayerDirective } from './on-loading-player.directive';

describe('OnLoadingPlayerDirective', () => {
  it('should create an instance', () => {
    const directive = new OnLoadingPlayerDirective();
    expect(directive).toBeTruthy();
  });
});
