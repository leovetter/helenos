import { FormatCoverPipe } from './format-cover.pipe';

describe('FormatCoverPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCoverPipe();
    expect(pipe).toBeTruthy();
  });
});
